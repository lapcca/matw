//! Binary entry point for MATW TUI

fn main() {
    // Simple entry point that delegates to the runner
    println!("MATW TUI is available through matw-cli");
    println!("Run: cargo run -p matw-cli");
}
